/// It's a class that holds the data for the dropdown
class TADropdownModel {
  /// Constructor
  TADropdownModel({
    required this.item,
    required this.id,
  });

  /// The [item] shown inside the dropdown
  final String item;

  /// The [id] of the iem
  final String id;
}
